import React from "react";
import CartProducts from "../Components/Cart/CartProducts";

const Carrito = () => {
  return (
    <div>
      <CartProducts />
    </div>
  );
};

export default Carrito;
